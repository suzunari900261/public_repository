AWSTemplateFormatVersion: 2010-09-09
Description:  Lambda Template for AWSIoT-demo

# ------------------------------------------------------------
# Parameters
# ------------------------------------------------------------
Parameters:
  ProjectName:
    Type: String
    Default: AWSIoT-demo

  Environment:
    Type: String
    Default: prod
    AllowedValues:
      - dev
      - prod

# ------------------------------------------------------------
# Resources
# ------------------------------------------------------------
Resources:
  Lambda:
    Type: AWS::Lambda::Function
    DeletionPolicy: Retain
    Properties:
      FunctionName: !Sub "${ProjectName}-${Environment}-Lambda"
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-${Environment}-Lambda"
        - Key: Environment
          Value: !Ref Environment
      Handler: lambda_function.lambda_handler
      Runtime: python3.12
      Role: !ImportValue AWSIoT-demo-prod-Lambda-role-Arn
      Timeout: 3
      MemorySize: 128
      EphemeralStorage:
        Size: 512
      Code:
        S3Bucket: suzu-cf-template
        S3Key: LambdaZIP/AWSIoT-demo/lambda_function.zip

  Permission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref Lambda
      Action: lambda:InvokeFunction
      Principal: iot.amazonaws.com
      SourceArn: !ImportValue AWSIoT-demo-prod-AWSIoT-Lambda-rule

# ------------------------------------------------------------
# Outputs
# ------------------------------------------------------------
Outputs:
  LambdaArn:
    Value: !GetAtt Lambda.Arn
    Export:
      Name: !Sub "${ProjectName}-${Environment}-Lambda"